{{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="btn-group" role="group" aria-label="Basic example">
    <button class="btn btn-success" type="submit">{{ button_label|default('Update') }}</button>
    <a href="category/_delete_form.html.twig" class="btn btn-primary">Delete</a>
    <a href="{{ path('app_category_index') }}" class="btn btn-primary">Back to list</a>
    </div>

{{ form_end(form) }}
